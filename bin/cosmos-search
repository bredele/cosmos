#!/usr/bin/env node

/**
 * Module dependencies.
 */

var program = require('commander');


// parse argv

program
  .usage('[query]')
  .parse(process.argv);


// query

var query = program.args.join(' ');

// console.log(query);